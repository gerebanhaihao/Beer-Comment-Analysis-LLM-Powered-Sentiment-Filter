# 2026-08-30

## git 数据安全策略调整（commit ed19c6f）

- 用户澄清：真实夸克源数据在 `data/`，`original/` 是不存在的过时规则。
- `.gitignore`：`original/` 替换为 `data/*` + `!data/.gitkeep`；`git rm --cached` 取消 `data/samples/` 两个 CSV 跟踪（本地文件保留）。
- `prepare` 命令默认 `--input-dir` 从 `original` 改为 `data`，与 `run` 一致。
- README 同步更新（目录说明、mock 演示命令、人审示例改用 data/）。
- 34 个 pytest 全绿。
- 注意：用户曾要求回退 benchmark 不入库的改动（commit 1c8ac93 已 reset），benchmark 仍保持 git 跟踪——用户对 benchmark 与 data 的入库策略态度不同，改动前先确认。

## API 密钥与运行环境

- DeepSeek key 存于根目录 `.env`（gitignored），`load_env_file()` 自动加载。
- 运行命令：`"/c/Program Files/Python312/python.exe" -m beer_sentiment run --all-time`（系统默认 python 3.13 无依赖）。
