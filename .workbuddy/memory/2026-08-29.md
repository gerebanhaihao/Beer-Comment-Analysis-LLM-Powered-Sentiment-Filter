# 2026-08-29

- 核验 beer-comment-analysis 项目与用户简历描述的一致性（用户正在求职/写简历）。
- 结论：双阶段管线（规则粗筛+LLM判定+低置信兜底+着色Excel+评测框架）与简历吻合；但 Hybrid RAG（仅README路线图M3）、痛点挖掘、营销活动追踪、舆情分级、720份标注（仓库仅25份合成benchmark）、真实模型评测结果（artifacts为空，模型配置为占位）在仓库中不存在。
- 关键位置：benchmark/beer_sentiment_benchmark.jsonl（25行）、README.md:78（RAG路线图）、src/beer_sentiment/rules/、pipeline/stage2.py、config/models.yaml。

## Hybrid RAG 落地（下午完成）

- 新增 `src/beer_sentiment/rag/`：tokenize（中文bigram）、sparse（BM25）、dense（字符n-gram特征哈希256维+余弦）、hybrid（RRF融合 + LLMReranker即LLM打分的Cross-Encoder重排，失败自动回退RRF）、judge（RagJudge装饰器把检索结果注入"参考上下文"）。
- 知识库 `config/knowledge_base.yaml`：9条判定规则 + 14条few-shot示例（blue/yellow/none含各类bad case）。配置在 `config/rag.yaml`。
- 模型只保留 mock + deepseek（deepseek-chat，base_url https://api.deepseek.com/v1），API key 在项目根 `.env`（已gitignore）。models.yaml default=deepseek。
- cli.py：`run` 默认 input=data、output=output；新增 `--all-time`（跳过时间窗）与 `--no-rag`；main 里 load_env_file()。
- stage2 支持 max_workers=4 并发判定（config/pipeline.yaml）。
- 端到端冒烟通过：37行样本→20候选→1黄，Excel着色正确；RAG检索验证 rerank 阶段生效（ex-own-quality score=1.0排第一）。34个pytest全过。
- 注意：本机 `python` 是无包的3.13，要用 `"/c/Program Files/Python312/python.exe"`（pip安装在此）。
- 用户目标工作流：原文件放 data/ → `beer-sentiment run --all-time` → output/ 出蓝黄着色Excel。
